# Copy to .env and fill in values
SUPABASE_URL=https://<PROJECT_REF>.supabase.co
SUPABASE_SERVICE_ROLE=<YOUR_SERVICE_ROLE_KEY>   # server-side only! never expose to frontend
SUPABASE_ANON_KEY=<OPTIONAL_FOR_EXPORT_ONLY>
REGISTRY_BUCKET=public/registry            # optional: storage bucket/path for exported json
